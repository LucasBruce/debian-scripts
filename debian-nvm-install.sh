curl -o- https://ghproxy.net/https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash

# NVM_ALIAS='# NVM configuration
# export NVM_DIR="$HOME/.nvm"
# [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"         # Load nvm
# [ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # Load nvm bash completion'

# echo "$NVM_ALIAS" >>$HOME/.zshrc
# echo "$NVM_ALIAS" >>~/.bashrc

# source ~/.zshrc
# source ~/.bashrc